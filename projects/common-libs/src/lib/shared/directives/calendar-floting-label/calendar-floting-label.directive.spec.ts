import { CalendarFlotingLabelDirective } from './calendar-floting-label.directive';

describe('CalendarFlotingLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarFlotingLabelDirective();
    expect(directive).toBeTruthy();
  });
});
