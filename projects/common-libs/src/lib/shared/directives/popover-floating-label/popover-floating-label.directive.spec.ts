import { PopoverFloatingLabelDirective } from './popover-floating-label.directive';

describe('OnFocusDirective', () => {
  it('should create an instance', () => {
    // const directive = new PopoverFloatingLabelDirective();
    // expect(directive).toBeTruthy();
  });
});
