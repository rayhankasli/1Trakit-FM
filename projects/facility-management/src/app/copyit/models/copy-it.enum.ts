/** CopyItPageType */
export enum CopyItPageType {
    BOTH = 1,
    COVER = 2,
    BACK = 3
}

/** Enum for CopyIt Page */
export enum CopyItPage {
    FRONT_PAGE = 1,
    MIDDLE_PAGE = 1
}

/** Enum for Rate Request Type */
export enum RateRequestType {
    CLIENT = 1,
    TENANT = 2
}

/** Enum for Allowed Requestor Section */
export enum AllowedRequestorSectionEnum {
    ENVELOP_AND_QUANTITY = 1,
    SHIPPING_METHOD = 2,
    PRIORITY_LEVEL = 3,
    DISTRIBUTION = 4,
    DATE_TIME_PROOF = 5
}

/** Enum for Priority Level */
export enum PriorityLevel {
    HIGH = 1,
    MEDIUM = 2,
    LOW = 3
}