import { DisableOnAjaxDirective } from './disable-on-ajax.directive';

describe('DisableOnAjaxDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOnAjaxDirective();
    expect(directive).toBeTruthy();
  });
});
